import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic5-sidemenu-tabs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
